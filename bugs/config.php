<?php
// ============================================================
//  تنظیمات میز باگ PayGoSafe
//  فقط همین فایل را ویرایش کنید.
// ============================================================
return [

    // --- تلگرام ---------------------------------------------------
    // توکن رباتی که از @BotFather گرفته‌اید
    'telegram_token'   => '',
    // آی‌دی گروه (عددی، معمولاً با -100 شروع می‌شود). برای پیدا کردنش
    // بعد از ورود به عنوان دولوپر، صفحه telegram.php را باز کنید.
    'telegram_chat_id' => '',

    // --- ورود -----------------------------------------------------
    // برای هر تیم یک نام کاربری و «هش» رمز عبور.
    // رمز به صورت خام ذخیره نمی‌شود. برای ساختن هش، صفحه hash.php را
    // در مرورگر باز کنید، رمز دلخواه را بنویسید و خروجی را اینجا بگذارید.
    'users' => [
        'support' => [
            'username'      => 'support',
            'password_hash' => '',
        ],
        'dev' => [
            'username'      => 'dev',
            'password_hash' => '',
        ],
    ],

    // --- رفتار سیستم ------------------------------------------------
    // آدرس کامل پوشه‌ی پنل (برای لینک داخل پیام‌های تلگرام). بدون / انتها.
    // خالی بگذارید تا خودکار تشخیص داده شود.
    'base_url' => '',

    // کدام تغییر وضعیت‌ها به چنل ارسال شوند (پیش‌فرض: همه)
    'notify_statuses' => ['review', 'info', 'test', 'reopen', 'closed', 'rejected'],
    // یادداشت‌ها (بدون تغییر وضعیت) به چنل ارسال شوند؟
    'notify_notes' => true,

    // آپلود
    'max_files'     => 3,
    'max_file_mb'   => 5,

    // امنیت ورود
    'login_max_attempts' => 5,     // بعد از این تعداد تلاش ناموفق
    'login_block_minutes' => 15,   // این مدت آی‌پی مسدود می‌شود
    'session_idle_minutes' => 720, // بعد از این مدت بی‌کاری خروج خودکار (۱۲ ساعت)

    'app_name' => 'میز باگ PayGoSafe',
];
